I am building a chat application (AgriAI Assistant) with a Flutter frontend and a Python FastAPI backend. Both are now running containerized inside Docker, using a DeepSeek model hosted via NVIDIA NIM.

### THE DOCKER TIME-OUT BUG:
1. When a user sends a complex request (e.g., "how solve leaves bleat"), the NIM API takes longer to process, which triggers a timeout.
2. The UI immediately falls back to displaying an offline/error placeholder text instead of a loading spinner.
3. Behind the scenes in the Docker backend container, the execution thread keeps running, completes the DeepSeek response, and saves the successful AI output into the database.
4. When the user manually refreshes the page, the offline text is replaced with the correct AI response from the database.
5. Short messages like "test" bypass this because the API responds fast enough to beat the network timeout.

### REQUIRED ARCHITECTURAL FIXES:

1. **Backend (FastAPI Container):** 
   - Modify the chat endpoint to accept the request, trigger the heavy NIM LLM generation asynchronously as a decoupled background task (e.g., using FastAPI's `BackgroundTasks`), and immediately return a `202 Accepted` status along with a `processing` state and the database message ID.
   - Ensure the server gateway configuration (e.g., Uvicorn command in the Dockerfile) doesn't kill worker processes early by bumping `--timeout-keep-alive 60`.
   - Create a fast status checking endpoint: `/api/v1/chat/status/{message_id}` that returns whether the AI response is `pending`, `processing`, or `completed`.

2. **Frontend (Flutter Web/App Container):**
   - When a user hits send, instantly append their message to the UI and show a loading/typing indicator (spinner) for the upcoming AI response block.
   - Check the network client configuration (Dio/Http). Ensure `connectTimeout` and `receiveTimeout` are set to at least 60 seconds to survive inter-container delay spikes.
   - If the backend returns a `processing` status, keep the spinner active and initiate a periodic timer (polling loop every 2 seconds) hitting the new `/status/{message_id}` endpoint.
   - As soon as the status changes to `completed`, stop the polling loop, fetch the fresh AI text output from the database, and replace the loading spinner with the actual text response dynamically without requiring a manual page refresh.

Please ask me to provide the relevant Dockerfile, backend router/service code, and frontend chat screen/controller files so we can implement these changes directly.