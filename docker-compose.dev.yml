services:
  backend:
    volumes:
      - ./backend:/app
    environment:
      - RELOAD=true
    command: ["sh", "-c", "cd /app && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --timeout-keep-alive 60"]

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
